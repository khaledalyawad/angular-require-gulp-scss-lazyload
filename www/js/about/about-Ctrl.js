'use strict';

define(['js/app'], function(app) {
  return app.$controllerProvider.register('aboutCtrl', [
    '$scope',
    '$rootScope',
    '$state',
    function($scope, $rootScope, $state) {

    }
  ]);
});
