'use strict';

define(['js/app'], function(app) {
  return app.$controllerProvider.register('startCtrl', [
    '$scope',
    '$rootScope',
    '$state',
    function($scope, $rootScope, $state) {

    }
  ]);
});
